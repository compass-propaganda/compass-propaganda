{
  "name": "compass-propaganda",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-oracle.mjs && node scripts/build-site.mjs",
    "test": "node --test scripts/*.test.mjs"
  },
  "engines": { "node": ">=20" },
  "devDependencies": {
    "markdown-it": "15.0.1",
    "markdown-it-footnote": "4.0.0"
  }
}
